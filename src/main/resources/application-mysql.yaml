spring:
  datasource:
#    hikari:
#      jdbc-url: mysql://localhost:3306/test?useSSL=false&characterEncoding=UTF-8
#      username: root
#      password: rootpassword
#      driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/test?useSSL=false&characterEncoding=UTF-8
    username: root
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
#      ddl-auto: update
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
logging:
  level:
    org.hibernate.SQL: info
    com.kkulkkeog: debug
    root: info
file:
  base-path: "./src/main/resources/upload/"